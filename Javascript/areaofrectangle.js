let length = 12 //declaration and initialization of variable
let breadth = 20 //declaration and initialization of variable
let prefix = "The area of rectangle having length " //declaration and initialization of variable
let area = (length * breadth) //declaration, initialization of variable and performing calculation
console.log(prefix + length + " and breadth " + breadth + " is " + area) // printing output
